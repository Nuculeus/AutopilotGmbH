# .claude/skills

This directory is intended for Paperclip-specific skill files or symlinks that Claude-compatible tooling can discover automatically.

Recommended use:

- symlink selected files from `custom-skills/`
- keep only Paperclip-specific wrappers here
- avoid duplicating canonical skill content when symlinks are sufficient
